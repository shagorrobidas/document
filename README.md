## Github command

`ls -a`
`pwd

```bash

echo "Hello, World!"

##git configurations :
global :
echo "git config --global user.name " user name""
git config --global user.email ""
git config --global --list

remov: 
git config --global --unset user.email ""
  
chack user list config :
git config --list

local configuration :
git config --local user.name ""
git config --local user.email ""
git config --local --list

check user and mail :
git config user.name 
git config user.email


ssh key :
sucure shall , one compute to another computer connect sucure 


public ssh key:
ssh-keygen -o -t rsa -C roysagor88@gmail.com
id_pub
id_rsa.pub


git status

git add .
git add -A
git add *.py only this extantion file add 
git **/*.js same extaintion same file 

git rm --cached filename  #remove change
git restore

git diff #chenge diff

git log #see all commit
git commit -m""
